{% comment %}
  ┌─────────────────────────────────────────────────────────────┐
  │  sections/custom-discovery-set.liquid                       │
  │  Split-layout section: image left, text right.              │
  │                                                             │
  │  Replicates the Mattheuse "NOT SURE YET? START WITH THE     │
  │  DISCOVERY SET." section with:                              │
  │    • 50/50 image + text columns (stacks on mobile)          │
  │    • H2 heading above scent list                            │
  │    • List of 3 scent names (rendered from blocks)           │
  │    • Single CTA button                                      │
  │                                                             │
  │  Docs:                                                      │
  │  • image_url / image_tag → shopify.dev/docs/api/liquid/    │
  │      filters/image_url                                      │
  │  • section schema → shopify.dev/docs/storefronts/themes/   │
  │      architecture/sections/section-schema                   │
  └─────────────────────────────────────────────────────────────┘
{% endcomment %}

{%- liquid
  assign image_side = section.settings.image_side
  assign section_bg = section.settings.background_color
  assign text_color = section.settings.text_color
  assign text_align = section.settings.text_alignment | default: 'left'
  assign mobile_text_align = section.settings.mobile_text_alignment | default: 'left'
  assign discovery_btn_url = section.settings.btn_url
  if discovery_btn_url == blank and section.settings.featured_product != blank
    assign discovery_btn_url = section.settings.featured_product.url
  endif
  if discovery_btn_url == blank and section.settings.featured_collection != blank
    assign discovery_btn_url = section.settings.featured_collection.url
  endif
  if discovery_btn_url == blank
    assign discovery_btn_url = routes.all_products_collection_url
  endif
-%}

<style>
  /* ── Wrapper ───────────────────────────────────────────── */
  #shopify-section-{{ section.id }} {
    background-color: {{ section_bg }};
    color: {{ text_color }};
    padding: {{ section.settings.padding_top }}px 0 {{ section.settings.padding_bottom }}px;
  }

  #shopify-section-{{ section.id }} .discovery__grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    min-height: {{ section.settings.min_height }}px;
    gap: {{ section.settings.column_gap }}px;
    {% if image_side == 'right' %}direction: rtl;{% endif %}
  }

  /* ── Image column ──────────────────────────────────────── */
  #shopify-section-{{ section.id }} .discovery__image-col {
    position: relative;
    overflow: hidden;
    {% if image_side == 'right' %}direction: ltr;{% endif %}
  }

  #shopify-section-{{ section.id }} .discovery__image {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  /* ── Text column ───────────────────────────────────────── */
  #shopify-section-{{ section.id }} .discovery__text-col {
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: {{ section.settings.content_padding_y }}px {{ section.settings.content_padding_x }}px;
    text-align: {{ text_align }};
    align-items: {% if text_align == 'center' %}center{% else %}flex-start{% endif %};
    {% if image_side == 'right' %}direction: ltr;{% endif %}
  }

  #shopify-section-{{ section.id }} .discovery__label {
    font-family: 'HelveticaNowDisplay-Medium', sans-serif;
    font-size: 12px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    opacity: 0.6;
    margin-bottom: 12px;
  }

  #shopify-section-{{ section.id }} .discovery__heading {
    font-family: 'HelveticaNowDisplay-ExtraBold', sans-serif;
    font-size: clamp(1.5rem, 2.8vw, 2.25rem);
    line-height: 1.25;
    margin: 0 0 24px;
    color: {{ text_color }};
  }

  #shopify-section-{{ section.id }} .discovery__description {
    font-family: 'HelveticaNowDisplay-Medium', sans-serif;
    font-size: 15px;
    line-height: 1.6;
    opacity: 0.8;
    margin-bottom: 28px;
  }

  #shopify-section-{{ section.id }} .discovery__description p {
    margin: 0 0 12px;
  }

  #shopify-section-{{ section.id }} .discovery__description p:last-child {
    margin-bottom: 0;
  }

  /* ── Scent list ────────────────────────────────────────── */
  #shopify-section-{{ section.id }} .discovery__scents {
    list-style: none;
    padding: 0;
    margin: 0 0 {{ section.settings.content_gap }}px;
    border-top: 1px solid rgba(0,0,0,0.12);
  }

  #shopify-section-{{ section.id }} .discovery__scent-item {
    display: flex;
    flex-direction: column;
    padding: 16px 0;
    border-bottom: 1px solid rgba(0,0,0,0.12);
  }

  #shopify-section-{{ section.id }} .discovery__scent-name {
    font-family: 'HelveticaNowDisplay-ExtraBold', sans-serif;
    font-size: 14px;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    margin-bottom: 2px;
  }

  #shopify-section-{{ section.id }} .discovery__scent-desc {
    font-family: 'HelveticaNowDisplay-Medium', sans-serif;
    font-size: 13px;
    opacity: 0.65;
  }

  /* ── CTA button ────────────────────────────────────────── */
  #shopify-section-{{ section.id }} .discovery__btn {
    display: inline-block;
    align-self: flex-start;
    padding: 12px 20px;
    background: {{ section.settings.btn_bg }};
    color: {{ section.settings.btn_text }};
    border: 2px solid {{ section.settings.btn_bg }};
    border-radius: 5px;
    font-family: 'HelveticaNowDisplay-ExtraBold', sans-serif;
    font-size: 16px;
    text-transform: uppercase;
    text-decoration: none;
    letter-spacing: 0.02em;
    cursor: pointer;
    transition: background 0.25s ease-out, border-color 0.25s ease-out;
  }

  #shopify-section-{{ section.id }} .discovery__btn:hover {
    background: #a6a6a6;
    border-color: #a6a6a6;
  }

  /* ── Mobile ────────────────────────────────────────────── */
  @media (max-width: 749px) {
    #shopify-section-{{ section.id }} .discovery__grid {
      grid-template-columns: 1fr;
      direction: ltr;          /* reset rtl used for right-image layout */
      gap: {{ section.settings.mobile_column_gap }}px;
    }

    #shopify-section-{{ section.id }} .discovery__image-col {
      min-height: {{ section.settings.mobile_image_height }}px;
      direction: ltr;
    }

    #shopify-section-{{ section.id }} .discovery__text-col {
      padding: {{ section.settings.mobile_content_padding_y }}px {{ section.settings.mobile_content_padding_x }}px;
      text-align: {{ mobile_text_align }};
      align-items: {% if mobile_text_align == 'center' %}center{% else %}flex-start{% endif %};
      direction: ltr;
    }

    #shopify-section-{{ section.id }} .discovery__btn {
      width: 100%;
      text-align: center;
    }
  }
</style>

<div class="discovery__grid">

  {%- comment -%} Image column {%- endcomment -%}
  <div class="discovery__image-col">
    {%- if section.settings.image != blank -%}
      {%- assign discovery_alt = section.settings.image.alt | default: section.settings.heading | escape -%}
      {{
        section.settings.image
        | image_url: width: 1200
        | image_tag:
          class: 'discovery__image',
          loading: 'lazy',
          alt: discovery_alt,
          widths: '400, 700, 900, 1200',
          sizes: '(max-width: 749px) 100vw, 50vw'
      }}
    {%- else -%}
      {%- comment -%}
        Placeholder when no image is set in theme editor.
        Ref: shopify.dev/docs/api/liquid/filters/placeholder_svg_tag
      {%- endcomment -%}
      {{ 'lifestyle-2' | placeholder_svg_tag: 'discovery__image' }}
    {%- endif -%}
  </div>

  {%- comment -%} Text column {%- endcomment -%}
  <div class="discovery__text-col">

    {%- if section.settings.label != blank -%}
      <p class="discovery__label">{{ section.settings.label | escape }}</p>
    {%- endif -%}

    {%- if section.settings.heading != blank -%}
      <h2 class="discovery__heading">{{ section.settings.heading | escape }}</h2>
    {%- endif -%}

    {%- if section.settings.description != blank -%}
      <div class="discovery__description">{{ section.settings.description }}</div>
    {%- endif -%}

    {%- comment -%}
      Scent blocks — each block represents one scent in the
      discovery set. block.shopify_attributes enables
      drag-to-reorder in the theme editor.
      Ref: shopify.dev/docs/api/liquid/objects/block
    {%- endcomment -%}
    {%- assign scent_blocks = section.blocks | where: "type", "scent" -%}
    {%- if scent_blocks.size > 0 -%}
      <ul class="discovery__scents">
        {%- for block in scent_blocks -%}
          <li class="discovery__scent-item" {{ block.shopify_attributes }}>
            <span class="discovery__scent-name">{{ block.settings.name | escape }}</span>
            {%- if block.settings.description != blank -%}
              <span class="discovery__scent-desc">{{ block.settings.description | escape }}</span>
            {%- endif -%}
          </li>
        {%- endfor -%}
      </ul>
    {%- endif -%}

    {%- if section.settings.btn_label != blank -%}
      <a href="{{ discovery_btn_url }}" class="discovery__btn">
        {{ section.settings.btn_label | escape }}
      </a>
    {%- endif -%}

  </div>
</div>

{% schema %}
{
  "name": "Discovery Set",
  "tag": "section",
  "class": "custom-discovery-set",
  "settings": [
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "select",
      "id": "image_side",
      "label": "Image position",
      "options": [
        { "value": "left",  "label": "Left" },
        { "value": "right", "label": "Right" }
      ],
      "default": "left"
    },
    {
      "type": "range",
      "id": "min_height",
      "label": "Desktop minimum height",
      "min": 320,
      "max": 900,
      "step": 20,
      "unit": "px",
      "default": 560
    },
    {
      "type": "range",
      "id": "column_gap",
      "label": "Desktop gap between image and content",
      "min": 0,
      "max": 80,
      "step": 4,
      "unit": "px",
      "default": 24
    },
    {
      "type": "select",
      "id": "text_alignment",
      "label": "Desktop text alignment",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "center", "label": "Center" }
      ],
      "default": "left"
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text color",
      "default": "#000000"
    },
    {
      "type": "header",
      "content": "Image"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Section image"
    },
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "label",
      "label": "Eyebrow label",
      "default": "For the undecided"
    },
    {
      "type": "textarea",
      "id": "heading",
      "label": "Heading",
      "default": "NOT SURE YET? START WITH THE DISCOVERY SET."
    },
    {
      "type": "richtext",
      "id": "description",
      "label": "Description",
      "default": "<p>Sample all three scents before committing to a full bottle.</p>"
    },
    {
      "type": "range",
      "id": "content_padding_x",
      "label": "Desktop horizontal content padding",
      "min": 16,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 72
    },
    {
      "type": "range",
      "id": "content_padding_y",
      "label": "Desktop vertical content padding",
      "min": 16,
      "max": 140,
      "step": 4,
      "unit": "px",
      "default": 60
    },
    {
      "type": "range",
      "id": "content_gap",
      "label": "Gap below scent list",
      "min": 8,
      "max": 72,
      "step": 4,
      "unit": "px",
      "default": 36
    },
    {
      "type": "header",
      "content": "Button"
    },
    {
      "type": "text",
      "id": "btn_label",
      "label": "Button label",
      "default": "SHOP DISCOVERY SET"
    },
    {
      "type": "url",
      "id": "btn_url",
      "label": "Button URL"
    },
    {
      "type": "product",
      "id": "featured_product",
      "label": "Fallback product",
      "info": "Used when button URL is blank"
    },
    {
      "type": "collection",
      "id": "featured_collection",
      "label": "Fallback collection",
      "info": "Used when button URL and fallback product are blank"
    },
    {
      "type": "color",
      "id": "btn_bg",
      "label": "Button background",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "btn_text",
      "label": "Button text color",
      "default": "#ffffff"
    },
    {
      "type": "header",
      "content": "Section spacing"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding top",
      "min": 0,
      "max": 160,
      "step": 4,
      "unit": "px",
      "default": 56
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding bottom",
      "min": 0,
      "max": 160,
      "step": 4,
      "unit": "px",
      "default": 56
    },
    {
      "type": "header",
      "content": "Mobile"
    },
    {
      "type": "select",
      "id": "mobile_text_alignment",
      "label": "Mobile text alignment",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "center", "label": "Center" }
      ],
      "default": "left"
    },
    {
      "type": "range",
      "id": "mobile_image_height",
      "label": "Mobile image minimum height",
      "min": 180,
      "max": 520,
      "step": 20,
      "unit": "px",
      "default": 300
    },
    {
      "type": "range",
      "id": "mobile_column_gap",
      "label": "Mobile gap between image and content",
      "min": 0,
      "max": 60,
      "step": 4,
      "unit": "px",
      "default": 20
    },
    {
      "type": "range",
      "id": "mobile_content_padding_x",
      "label": "Mobile horizontal content padding",
      "min": 8,
      "max": 40,
      "step": 2,
      "unit": "px",
      "default": 16
    },
    {
      "type": "range",
      "id": "mobile_content_padding_y",
      "label": "Mobile vertical content padding",
      "min": 8,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 40
    }
  ],
  "blocks": [
    {
      "name": "Scent",
      "type": "scent",
      "limit": 6,
      "settings": [
        {
          "type": "text",
          "id": "name",
          "label": "Scent name",
          "default": "GENTLEMAN"
        },
        {
          "type": "text",
          "id": "description",
          "label": "Short descriptor",
          "default": "Bold · Magnetic · Modern"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Discovery Set",
      "category": "Product",
      "settings": {
        "heading": "NOT SURE YET? START WITH THE DISCOVERY SET.",
        "label": "For the undecided",
        "description": "<p>Sample all three scents before committing to a full bottle.</p>",
        "btn_label": "SHOP DISCOVERY SET"
      },
      "blocks": [
        { "type": "scent", "settings": { "name": "GENTLEMAN",       "description": "Bold · Magnetic · Modern"          } },
        { "type": "scent", "settings": { "name": "LUMIÈRE DE JOUR", "description": "Fresh · Clean · Professional"      } },
        { "type": "scent", "settings": { "name": "NOIR SILLAGE",    "description": "Deep · Warm · Unforgettable"       } }
      ]
    }
  ]
}
{% endschema %}
